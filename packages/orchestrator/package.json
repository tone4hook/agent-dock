{
  "name": "@agent-dock/orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agent-dock/agents": "file:../agents",
    "@agent-dock/db": "file:../db",
    "@agent-dock/shared": "file:../shared",
    "@agent-dock/workflows": "file:../workflows",
    "@agent-dock/worktrees": "file:../worktrees"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "better-sqlite3": "^12.2.0",
    "typescript": "^5.8.3",
    "vitest": "^2.1.0"
  }
}
