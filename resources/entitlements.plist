<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Hardened-runtime entitlements for the agent-dock .app.

    - allow-jit / allow-unsigned-executable-memory: required by V8
      (Node + WKWebView) for JIT-compiled JavaScript.
    - disable-library-validation: we ship our own node_modules,
      including unsigned native modules (better-sqlite3, keytar,
      tree-kill, etc.); the system would otherwise refuse to load
      them under a hardened runtime.
    - allow-dyld-environment-variables: we set NODE_PATH on the
      embedded Node child process.
  -->
  <key>com.apple.security.cs.allow-jit</key><true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key><true/>
  <key>com.apple.security.cs.disable-library-validation</key><true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key><true/>
</dict>
</plist>
